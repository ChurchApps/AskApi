# AskApi Playground Environment Variables
# Copy this file to .env and fill in your values

# Stage (development, staging, production)
REACT_APP_STAGE=development

# API URLs (optional - will use defaults based on stage if not provided)
# Development defaults to localhost URLs
# Staging/Production use churchapps.org URLs
REACT_APP_MEMBERSHIP_API=https://api.staging.churchapps.org/membership
REACT_APP_ASK_API=http://localhost:8097